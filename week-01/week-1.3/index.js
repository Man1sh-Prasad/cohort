// for each

const initialArray = [1, 2, 4];

function logThing(str){
    console.log(str);
}

initialArray.forEach(logThing);

// above code is same as
    // logThing(1)
    // logThing(2)
    // logThing(4)